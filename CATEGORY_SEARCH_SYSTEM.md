# 🔍 Система поиска по категориям

## 📋 Описание

Универсальная система поиска по категориям для Telegram бота, которая позволяет легко добавлять новые категории и настраивать поиск по ключевым словам.

## 🏗️ Архитектура

### CategoryManager.java
Основной класс для управления категориями и поиском:

```java
// Словарь категорий с ключевыми словами
Map<String, List<String>> CATEGORY_KEYWORDS = {
    "Гаш/Шиш 🍫🥦": ["гаш", "шиш", "hash", "weed", "марихуана"],
    "Cox 🥥": ["coke", "cocaine", "кокаин", "кокс"],
    "LSD 🍭🍄": ["lsd", "acid", "trip", "грибы", "псилоцибин"],
    "❄️⚡": ["ice", "meth", "speed", "лед", "скорость"],
    "💊💎": ["mdma", "pill", "xtc", "экстази", "мефедрон"]
}
```

## 🚀 Основные методы

### 1. Поиск по категории
```java
List<Product> searchByCategory(String categoryName)
```
- **Входные данные**: название категории
- **Возвращает**: список найденных товаров
- **Логика**: ищет товары по ключевым словам категории

### 2. Поиск по запросу
```java
List<Product> searchByQuery(String query)
```
- **Входные данные**: поисковый запрос пользователя
- **Возвращает**: список найденных товаров
- **Логика**: ищет товары по названию и описанию

### 3. Получение всех категорий
```java
List<String> getAllCategories()
```
- **Возвращает**: список всех доступных категорий

### 4. Добавление новой категории
```java
void addCategory(String categoryName, List<String> keywords)
```
- **Входные данные**: название категории и список ключевых слов
- **Функция**: добавляет новую категорию в систему

## 📝 Примеры использования

### Добавление новой категории
```java
// Добавляем новую категорию "Каннабис"
List<String> cannabisKeywords = Arrays.asList("каннабис", "cannabis", "марихуана", "травка");
CategoryManager.addCategory("Каннабис 🌿", cannabisKeywords);
```

### Поиск товаров
```java
// Поиск по категории
List<Product> hashProducts = CategoryManager.searchByCategory("Гаш/Шиш 🍫🥦");

// Поиск по запросу
List<Product> searchResults = CategoryManager.searchByQuery("гашиш");
```

## 🎯 Преимущества системы

1. **Легкость добавления категорий** - просто добавьте новую запись в словарь
2. **Гибкий поиск** - поддержка множественных ключевых слов
3. **Масштабируемость** - легко добавлять новые товары и категории
4. **Универсальность** - работает как с категориями, так и с произвольными запросами
5. **Производительность** - быстрый поиск по предустановленным ключевым словам

## 🔧 Настройка

### Добавление новых товаров
Отредактируйте метод `initializeMockProducts()` в `CategoryManager.java`:

```java
MOCK_PRODUCTS.add(new Product(
    id, 
    "Название товара", 
    "Описание товара", 
    price, 
    stock, 
    "Категория"
));
```

### Добавление новых категорий
Добавьте новую запись в `CATEGORY_KEYWORDS`:

```java
CATEGORY_KEYWORDS.put("Новая категория 🎯", Arrays.asList("ключ1", "ключ2", "ключ3"));
```

## 🎮 Использование в боте

1. Пользователь нажимает кнопку "🔍 Поиск по категориям"
2. Выбирает категорию из списка
3. Система ищет товары по ключевым словам категории
4. Отображает найденные товары с подробной информацией

## 📊 Структура данных

### Product
```java
class Product {
    int id;
    String name;
    String description;
    int price;
    int stock;
    String category;
}
```

### Категория
```java
Map<String, List<String>> {
    "Название категории": ["ключевое_слово1", "ключевое_слово2", ...]
}
```

## 🚀 Готово к использованию!

Система полностью интегрирована в бота и готова к работе. Все изменения применяются автоматически при перезапуске бота.

