# Интеграция с Java Back-end API

## Описание

Telegram бот теперь полностью интегрирован с вашим Java Back-end API. Все торговые операции, получение цен и статистика работают через ваш Back-end.

## Конфигурация

### Настройка в application.properties

Добавьте следующие параметры в `src/main/resources/application.properties`:

```properties
# Back-end API Configuration
backend.api.url=http://localhost:8080/api
backend.api.key=YOUR_API_KEY
```

**Параметры:**
- `backend.api.url` - базовый URL вашего Back-end API (по умолчанию: `http://localhost:8080/api`)
- `backend.api.key` - API ключ для аутентификации (опционально)

## API Endpoints

Ваш Back-end должен предоставлять следующие endpoints:

### 1. Получение цены
```
GET /api/trading/price/{symbol}
```

**Параметры:**
- `symbol` - торговый символ (например, `BTC-USDT`, `ETH-USDT`)

**Ответ:**
```json
{
  "price": 45000.50
}
```
или
```json
{
  "data": {
    "price": 45000.50
  }
}
```

### 2. Получение истории цен
```
GET /api/trading/history/{symbol}?minutes=15
```

**Параметры:**
- `symbol` - торговый символ
- `minutes` - количество минут истории

**Ответ:**
```json
{
  "data": [
    {
      "timestamp": 1704960000000,
      "price": 45000.00
    },
    {
      "timestamp": 1704960600000,
      "price": 45050.00
    }
  ]
}
```

### 3. Выполнение торговой операции
```
POST /api/trading/execute
```

**Тело запроса:**
```json
{
  "symbol": "BTC-USDT",
  "action": "BUY",
  "amount": 0.001,
  "price": 45000.00
}
```

**Ответ:**
```json
{
  "success": true,
  "tradeId": "TRADE_1234567890",
  "message": "Trade executed successfully"
}
```

### 4. Получение баланса
```
GET /api/trading/balance
```

**Ответ:**
```json
{
  "balance": 10000.00,
  "availableBalance": 9500.00
}
```
или
```json
{
  "data": {
    "balance": 10000.00,
    "availableBalance": 9500.00
  }
}
```

### 5. Получение статистики торговли
```
GET /api/trading/stats
```

**Ответ:**
```json
{
  "data": {
    "totalTrades": 100,
    "profitableTrades": 65,
    "losingTrades": 35,
    "totalProfit": 15.5,
    "avgProfit": 0.155,
    "winRate": 65.0,
    "balance": 11550.00,
    "availableBalance": 11000.00
  }
}
```

### 6. Получение открытых сделок
```
GET /api/trading/trades/open
```

**Ответ:**
```json
{
  "data": [
    {
      "id": "TRADE_123",
      "symbol": "BTC-USDT",
      "type": "BUY",
      "amount": 0.001,
      "entryPrice": 45000.00,
      "entryTime": "2024-01-11T12:00:00"
    }
  ]
}
```

## Аутентификация

Если ваш Back-end требует аутентификацию, API ключ передается в заголовках:
- `Authorization: Bearer {apiKey}`
- `X-API-Key: {apiKey}`

## Приоритет источников данных

Система использует следующий приоритет:

1. **Back-end API** (приоритетный источник)
   - Все торговые операции
   - Получение цен
   - Получение баланса
   - Статистика

2. **Резервные источники** (если Back-end недоступен)
   - Binance API (для цен)
   - CoinGecko API (для цен)
   - Локальное кэширование

## Обработка ошибок

Система автоматически обрабатывает ошибки:
- Если Back-end недоступен, используются резервные источники
- Логируются все ошибки для отладки
- Локальное кэширование обеспечивает работу при временных сбоях

## Примеры использования

### Получение цены через Back-end
```java
Double price = BackendApiClient.getPrice("BTC-USDT");
```

### Выполнение сделки через Back-end
```java
BackendApiClient.TradeExecutionResult result = BackendApiClient.executeTrade(
    "BTC-USDT", "BUY", 0.001, 45000.00
);
if (result.success) {
    System.out.println("Сделка выполнена: " + result.tradeId);
}
```

### Получение баланса
```java
Double balance = BackendApiClient.getBalance();
```

## Тестирование

Для тестирования без Back-end:
1. Не указывайте `backend.api.url` в конфигурации
2. Система автоматически переключится на резервные источники
3. Используются тестовые данные для разработки

## Важные замечания

1. **Формат символов**: Back-end ожидает символы в формате `BTC-USDT` (с дефисом), а не `BTC/USDT`
2. **Таймауты**: Запросы к Back-end имеют таймаут 5 секунд
3. **Кэширование**: Цены кэшируются на 5 секунд для оптимизации
4. **Обратная совместимость**: Если Back-end недоступен, система продолжит работать с резервными источниками

## Настройка для продакшена

1. Укажите правильный URL вашего Back-end API
2. Настройте API ключ для безопасности
3. Убедитесь, что все endpoints возвращают данные в ожидаемом формате
4. Протестируйте все операции перед запуском

Система готова к работе с вашим Java Back-end! 🚀
