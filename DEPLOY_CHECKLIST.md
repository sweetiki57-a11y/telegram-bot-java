# Чеклист перед деплоем

## ✅ Проверки выполнены

### 1. Компиляция
- ✅ Проект компилируется без ошибок
- ✅ Все 42 Java файла на месте
- ✅ JAR файл собран (11MB)

### 2. Тесты
- ✅ Все 78 тестов проходят успешно
- ✅ 0 ошибок, 0 пропущенных

### 3. Компоненты
- ✅ TelegramBotApplication - главный класс
- ✅ MyTelegramBot - основной бот
- ✅ AutoTradingEngine - автоматическая торговля
- ✅ WalletService - работа с кошельком
- ✅ SendTradingCommand - команда торговли
- ✅ SendWalletCommand - команда кошелька
- ✅ Все торговые стратегии
- ✅ BackendApiClient - интеграция с Back-end

### 4. Конфигурация
- ✅ application.properties существует
- ✅ Токен бота настроен
- ⚠️  Back-end API URL нужно настроить

## 📋 Перед деплоем

### Обязательно настроить:

1. **application.properties** (`src/main/resources/application.properties`):
   ```properties
   telegram.bot.username=YOUR_BOT_USERNAME
   telegram.bot.token=YOUR_BOT_TOKEN
   backend.api.url=http://your-backend:8080/api
   backend.api.key=YOUR_API_KEY
   ```

2. **Back-end API** должен быть доступен и предоставлять endpoints:
   - `/api/trading/price/{symbol}` - получение цен
   - `/api/trading/execute` - выполнение сделок
   - `/api/trading/balance` - баланс
   - `/api/wallet/deposit` - пополнение
   - `/api/wallet/withdraw` - вывод
   - `/api/wallet/balance` - баланс кошелька
   - `/api/wallet/transactions` - история

## 🚀 Запуск

### Локальный запуск:
```bash
./run.sh
```

### Перезапуск:
```bash
./restart.sh
```

### Проверка готовности:
```bash
./check.sh
```

### Ручной запуск:
```bash
mvn clean package
java -jar target/telegram-bot-1.0.0.jar
```

## 📦 Деплой на платформы

### Heroku:
```bash
git push heroku main
```

### Railway:
- Автоматический деплой при push в репозиторий
- Или через Railway CLI

### Docker:
```bash
docker build -t telegram-bot .
docker run -d telegram-bot
```

## 🔍 Проверка после деплоя

1. ✅ Бот отвечает на /start
2. ✅ Меню отображается корректно
3. ✅ Кнопка "🤖 Авто-торговля" работает
4. ✅ Кнопка "💰 Кошелек" работает
5. ✅ Пополнение баланса работает
6. ✅ Автоматическая торговля запускается
7. ✅ Back-end API отвечает

## 📊 Статистика проекта

- **Java файлов**: 42
- **Тестов**: 78 (все проходят)
- **Размер JAR**: 11MB
- **Зависимости**: Telegram Bot API, Jackson, SLF4J
- **Версия Java**: 11

## ⚠️ Важные замечания

1. **Безопасность**: Не коммитьте `application.properties` с реальными токенами
2. **Back-end**: Убедитесь что Back-end API доступен и настроен
3. **Баланс**: Начните с небольшого баланса для тестирования
4. **Мониторинг**: Следите за логами после деплоя

## 🎯 Готово к деплою!

Все проверки пройдены. Проект готов к запуску в продакшене.
